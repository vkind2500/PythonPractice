'''
Two sets are disjoint when they have no elements in common.
In other words, two disjoint sets are sets whose intersection is an empty set.
'''

odd_numbers = {1, 3, 5}
even_numbers = {2, 4, 6}

print(set.isdisjoint(odd_numbers,even_numbers))
